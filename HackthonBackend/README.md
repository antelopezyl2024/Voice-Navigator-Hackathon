Environments:
  JDK 17, Maven
  
Backend start up:
  1. clone repo
  2. open in IntelliJ / VS Code
  3. Maven sync
  4. run VoiceNavigatorApplication.java
  5. backend runs on http://localhost:8080

API:
  POST http://localhost:8080/api/query

Sample request/response, pls refer to API&JSON.md.
